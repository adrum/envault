includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - app

    # The level 9 is the highest level
    level: 5

    ignoreErrors:
        -
            identifier: return.type
            path: app/Models/Variable.php

    excludePaths:
        - ./*/*/FileToBeExcluded.php
